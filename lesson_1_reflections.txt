How did viewing a diff between two versions of a file help you see the bug that was introduced?

diff between two files will tell us the differences between the two files, and thus will be able to easily compare and pinpoint what we did wrong that introduced the bugs.

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?

The entire history of a file will enable a programmer to keep track of changes made in any instant of time, and reduce chances of bugs in the code.

How can you use the commands git log and git diff to view the history of files?

We can use git log to view the complete history of a git repository, and git diff can be used to find
difference between commits.

How might using version control make you more confident to make changes that could break something?

because we know that we can recover to the previouse state any time we want.

Now that you have your workspace set up, what do you want to try using Git for?

currently I am only learning it....will use it for future projects